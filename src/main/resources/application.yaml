spring:
  application:
    name: biblioteca
  security:
    user:
      name: admin
      password: minuevaClave#26875


function:
  azure:
    url:
      base:  https://func-biblioteca-djfmfvaacbfvdcfe.eastus2-01.azurewebsites.net/api/

event:
  grid:
    topic-endpoint: https://event-biblioteca.eastus-1.eventgrid.azure.net/api/events
    key: ${EVENT_GRID_KEY}
    tipo:
      evento:
        envio-correo: Envio.Correo
        generar-comprobante: Generar.Comprobante

server:
  port: 8080

#logging:
#  level:
#    root: DEBUG
